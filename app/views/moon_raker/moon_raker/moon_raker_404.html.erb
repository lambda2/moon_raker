<h1 class='page-header'>
  <%= t('moon_raker.oops') %>
  <small>
  <% if params[:method].blank? %>
    <%= t('moon_raker.resource_not_found_html', :resource => "<code>#{h(params[:resource])}</code>".html_safe) %>
  <% else %>
    <%= t('moon_raker.method_not_found_html', :resource => "<code>#{h(params[:resource])}</code>".html_safe,
        :method => "<code>#{h(params[:method])}</code>".html_safe) %>
  <% end %>
  </small>
</h1>

<% if @doc %>
  <%= t('moon_raker.goto_homepage_html', :href => link_to(
    t('moon_raker.goto_homepage_href', :app_name => @doc[:name]),
    File.join(@doc[:doc_url], @doc[:link_extension]))) %>
<% end %>
